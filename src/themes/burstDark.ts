import type { CommandPaletteTheme } from './types';
import { baseDarkVariables } from './base';

export const burstDarkTheme: CommandPaletteTheme = {
  id: 'burst-dark',
  name: 'Burst Dark',
  description: 'Default glassy dark command palette.',
  previewUrl: 'https://example.com',
  variables: baseDarkVariables,
};
