import type { CommandPaletteTheme } from './types';
import { baseLightVariables } from './base';

export const burstLightTheme: CommandPaletteTheme = {
  id: 'burst-light',
  name: 'Burst Light',
  description: 'Default light command palette.',
  appearance: 'light',
  previewUrl: 'https://example.com',
  variables: baseLightVariables,
};
